/**
 * Duplicate of tests in `src/utils/__tests__/seo-helpers.test.ts`.
 * Intentionally left without test definitions to avoid double execution.
 * (Kept temporarily in case any tooling references this path.)
 */
export {}; // no-op
